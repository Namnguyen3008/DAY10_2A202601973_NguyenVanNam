# BÁO CÁO ĐÓNG GÓP CÁ NHÂN (INDIVIDUAL REPORT)

* **Họ và tên**: Lê Kim Tính
* **Mã học viên (MSSV)**: `2A202601560`
* **Lớp**: 2A2026
* **Vai trò phụ trách**: Kỹ sư đánh giá & kiểm định chất lượng RAG (QA Evaluation Developer)

---

## 1. Nhiệm vụ & Vai trò phụ trách
Đảm nhận khâu **Evaluation Set Generation** (Tạo bộ câu hỏi đánh giá) và **RAG Metrics Scoring** (Tính toán chỉ số chất lượng):
* Viết cơ chế tự động xây dựng bộ câu hỏi chuẩn từ dữ liệu bài báo.
* Triển khai giải pháp chấm điểm câu trả lời qua Token F1 và LLM Judge.

## 2. Mô-đun mã nguồn đảm nhiệm
* **Mô-đun kiểm định**:
  - `src/evaluation/testset.py`
  - `src/evaluation/metrics.py`
* **Thư mục đầu ra**: `data/eval/` (Chứa `test_set.json`)

## 3. Kết quả thực hiện & Đóng góp chi tiết
* **Phát triển bộ sinh Test Set**: Viết logic tự sinh 32 câu hỏi đa dạng dựa trên metadata của bài báo học thuật gốc (tóm tắt, ngày tháng, tên tác giả, danh mục) kèm đáp án chuẩn (ground truth).
* **LLM Judge & Token F1**: Thiết kế prompt chấm điểm câu trả lời qua mô hình Gemini 3.1 & 3.5, chấm điểm dựa trên độ chính xác và tính đầy đủ thông tin.
* **Song song hóa tác vụ đánh giá**: Triển khai đa luồng `ThreadPoolExecutor` chạy 14 workers đồng thời để chấm điểm 32 câu testset, giảm thời gian đánh giá RAG từ **3 phút xuống còn 12 giây**.

## 4. Tự đánh giá mức độ hoàn thành
* **Kết quả**: Hệ thống đánh giá hoạt động nhanh và phản ánh chính xác sự chênh lệch chất lượng RAG giữa baseline sạch và dữ liệu lỗi.
* **Độ hoàn thành**: **100%**
